import type { Skill } from '../types/race';

export const SKILLS: Skill[] = [
  {
    id: 'tailwind',
    name: '순풍 질주',
    description: '레이스 초반, 바람을 타고 빠르게 치고 나간다',
    trigger: 'race_start',
    triggerCondition: (_state, progress) => progress < 0.2 && Math.random() < 0.6,
    duration: 30,
    effectType: 'speed_boost',
    effectValue: 1.4,
    cooldown: 999,
    rarity: 'common',
    color: '#60a5fa',
    icon: '💨',
  },
  {
    id: 'last_spurt',
    name: '막판 스퍼트',
    description: '결승선이 가까워지면 폭발적인 가속을 보여준다',
    trigger: 'final_stretch',
    triggerCondition: (_state, progress) => progress >= 0.75 && Math.random() < 0.7,
    duration: 40,
    effectType: 'burst',
    effectValue: 1.7,
    cooldown: 999,
    rarity: 'epic',
    color: '#f59e0b',
    icon: '🔥',
  },
  {
    id: 'front_runner',
    name: '선두 본능',
    description: '1위로 달리는 동안 페이스를 유지한다',
    trigger: 'leading',
    triggerCondition: (_state, _progress, rank) => rank === 1 && Math.random() < 0.5,
    duration: 50,
    effectType: 'speed_boost',
    effectValue: 1.2,
    cooldown: 200,
    rarity: 'rare',
    color: '#fbbf24',
    icon: '👑',
  },
  {
    id: 'comeback',
    name: '역전의 발걸음',
    description: '뒤처졌을 때 불굴의 투지로 순위를 끌어올린다',
    trigger: 'trailing',
    triggerCondition: (_state, progress, rank, total) =>
      progress > 0.25 && rank > Math.ceil(total * 0.55) && Math.random() < 0.6,
    duration: 45,
    effectType: 'acceleration_boost',
    effectValue: 1.5,
    cooldown: 300,
    rarity: 'rare',
    color: '#a78bfa',
    icon: '⚡',
  },
  {
    id: 'stamina_wave',
    name: '체력 파동',
    description: '고갈된 스태미나를 순간적으로 회복한다',
    trigger: 'midrace',
    triggerCondition: (state, progress) =>
      progress > 0.4 && state.staminaLeft < 50 && Math.random() < 0.6,
    duration: 20,
    effectType: 'stamina_recover',
    effectValue: 30,
    cooldown: 400,
    rarity: 'common',
    color: '#34d399',
    icon: '💚',
  },
  {
    id: 'iron_will',
    name: '철의 의지',
    description: '어떤 방해에도 흔들리지 않고 페이스를 유지한다',
    trigger: 'overtaken',
    triggerCondition: (state) => state.rank > state.lastRank && Math.random() < 0.7,
    duration: 35,
    effectType: 'resist_slowdown',
    effectValue: 0.8,
    cooldown: 250,
    rarity: 'common',
    color: '#94a3b8',
    icon: '🛡️',
  },
  {
    id: 'sonic_dash',
    name: '소닉 대시',
    description: '짧지만 폭발적인 최고속 질주를 펼친다',
    trigger: 'random',
    triggerCondition: (_state, progress) => progress > 0.3 && progress < 0.8 && Math.random() < 0.2,
    duration: 18,
    effectType: 'burst',
    effectValue: 1.75,
    cooldown: 550,
    rarity: 'epic',
    color: '#f43f5e',
    icon: '🌪️',
  },
  {
    id: 'rhythm_step',
    name: '리듬 스텝',
    description: '경쾌한 리듬에 맞춰 꾸준히 속도를 올린다',
    trigger: 'midrace',
    triggerCondition: (_state, progress) => progress > 0.3 && progress < 0.7 && Math.random() < 0.45,
    duration: 60,
    effectType: 'acceleration_boost',
    effectValue: 1.25,
    cooldown: 300,
    rarity: 'common',
    color: '#fb923c',
    icon: '🎵',
  },
  {
    id: 'starfall',
    name: '별의 소나기',
    description: '별빛이 쏟아지듯 잠깐의 마법 같은 가속을 보인다',
    trigger: 'random',
    triggerCondition: (_state, progress) => progress > 0.5 && Math.random() < 0.2,
    duration: 25,
    effectType: 'burst',
    effectValue: 1.8,
    cooldown: 600,
    rarity: 'epic',
    color: '#c084fc',
    icon: '✨',
  },
  {
    id: 'wind_rider',
    name: '바람 라이더',
    description: '코너를 돌며 가속력을 극대화한다',
    trigger: 'sprinting',
    triggerCondition: (_state, progress) => (progress > 0.2 && progress < 0.5) && Math.random() < 0.4,
    duration: 35,
    effectType: 'speed_boost',
    effectValue: 1.35,
    cooldown: 350,
    rarity: 'rare',
    color: '#2dd4bf',
    icon: '🌬️',
  },
];

export function getRandomSkills(count: number): Skill[] {
  const shuffled = [...SKILLS].sort(() => Math.random() - 0.5);
  return shuffled.slice(0, Math.min(count, SKILLS.length));
}

export function getSkillById(id: string): Skill | undefined {
  return SKILLS.find((s) => s.id === id);
}
